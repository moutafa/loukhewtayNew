<%= form_for(@categorie_event) do |f| %>
  <% if @categorie_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorie_event.errors.count, "error") %> prohibited this categorie_event from being saved:</h2>

      <ul>
      <% @categorie_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :libelle %><br />
    <%= f.text_field :libelle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
